"""The Trader — Binance USDⓈ-M Futures trading bot (testnet-first).

Backend package. FastAPI app + async broker + risk engine + signal engines
+ bot manager + SQLite persistence. Testnet by default; safe-mode boot with no
API keys configured.
"""

__version__ = "0.1.0"
